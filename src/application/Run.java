package application;

import java.util.*;

public class Run {

	public static void main(String[] args) {

		Locale.setDefault(Locale.US);
		Scanner in = new Scanner(System.in);
		
		/* O que preciso fazer:
		 * 
		 * Ajeitar a estrutura e lógicas presentes na classe Run
		 * Criar a classe estática "Menus" que servirá entre intermediário das classes e Run
		 * fazer lógica de add e remover contas (ambos os tipos)
		 * tratar exceções
		 * relacionar os produtos com estoque de forma que o carrinho só possa ter produtos do
		 * estoque.
		 * 
		 */
		

		in.close();
	}

}
